# Dependencies are managed by pixi.
# See pixi.toml (environment + tasks) and pyproject.toml (package metadata + runtime deps).
#
# Quick start:
#   pixi install       # resolve and install the environment
#   pixi run test      # run tests
#   pixi run serve     # start the dev server
